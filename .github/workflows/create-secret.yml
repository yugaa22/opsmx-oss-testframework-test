name: secret create
on:
  push:
    branches: [ "main" ]
jobs:
  secret:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      - name: List files in the repository
        run: |
          touch myfile.txt
          echo hiiii > myfile.txt
          #ls ${{ github.workspace }}  
          git config --global user.email "yugaa22@gmail.com"
          git config --global user.name "yugaa22"
          export GITHUB_TOKEN=""
          echo ${{ secrets.GITHUB_TOKEN }} | gh auth login --with-token
          gh alias list
          #gh secret list 
          #gh api -H "Accept: application/vnd.github+json" -H "X-GitHub-Api-Version: 2022-11-28"  /repos/opsmx-public/opsmx-oss-testframework/actions/secrets
          gh secret set MYSECRET < myfile.txt
          #git add .
          #git commit -m "Developper_commits"
          #git push
          #cat Dev_commits
        env:
           GITHUB_TOKEN: ${{ secrets.GIT_TOKEN }}
      - run: echo "🍏 This job's status is ${{ job.status }}."
