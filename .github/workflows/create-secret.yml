name: secret create
on:
  push:
    branches: [ "main" ]
jobs:
  secret:
    runs-on: ubuntu-latest
    steps:
      - run: gh secret list
        env:
          GITHUB_TOKEN: ${{ secrets.GIT_TOKEN }}
