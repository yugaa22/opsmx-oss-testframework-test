name: List Namespaces
on:
  push:
    branches:
      - main

jobs:
  list-namespaces:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Kubernetes
        uses: azure/k8s-set-context@v1
        with:
          kubeconfig: ${{ secrets.KUBECONFIG }}

      - name: List Namespaces
        run: kubectl get namespaces

      - name: Set up Helm
        uses: azure/setup-helm@v1
        with:
          helm-version: '3.6.3'  # Specify the version of Helm you want to use 
          kubeconfig: ${{ secrets.KUBECONFIG }}

      - name: Install Helm
        run: |
          curl -sSL https://raw.githubusercontent.com/helm/helm/master/scripts/get-helm-3 | bash

      - name: Add Helm repository
        run: |
          helm repo add stable https://charts.helm.sh/stable
          helm repo add spinnaker https://opsmx.github.io/spinnaker-helm/
          helm repo update
          kubectl get ns

      - name: Install Helm chart
        run: |
          
          helm upgrade --install oss-spin spinnaker/spinnaker -n opsmx-oss --timeout 600s --create-namespace


