## WARNING
## This file was autogenerated, and _will_ be overwritten by Halyard.
## Any edits you make here _will_ be lost.

spectator:
  applicationName: ${spring.application.name}
  webEndpoint:
    enabled: false

spinnaker:
  extensibility:
    plugins: {}
    repositories: {}
    plugins-root-path: /opt/front50/plugins
    strict-plugin-loading: false
  azs:
    enabled: false
  gcs:
    rootFolder: front50
    bucketLocation: ''
    enabled: false
  redis:
    host: oss-spin-redis-master
    port: 6379
    enabled: false
  s3:
    bucket: spinnaker
    rootFolder: front50
    pathStyleAccess: true
    endpoint: http://oss-spin-minio:9000
    accessKeyId: spinnakeradmin
    secretAccessKey: spinnakeradmin
    enabled: true
  oracle:
    enabled: false

server:
  port: ${services.front50.port:8080}
  address: ${services.front50.host:localhost}
